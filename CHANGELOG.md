# Changelog

## 1.0.3
- Fix CustomUI load failures on updated game assets by removing bundled `Common.ui` and `Sounds.ui` overrides.

## 1.0.1
- Fix offline wipe spawns by redirecting players who were offline during a wipe to the protected world.
- Add offline wipe tracking options (enable/disable, persistence, pruning, tracking mode).

## 1.0.0
- Initial release.
